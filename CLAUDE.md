自在律 開発ガイド

keirin_logic.js に触れる前に必ず最初に docs/keirin_logic_dependency_map.md を読むこと。
全体読み込み禁止。dependency_map.md の行番号でピンポイント読みのみ許可。

## リプレイ台（模擬機）— 係数・ロジック変更の事前計測義務【恒久ルール】

場所: このリポジトリの `replay/` 配下（仕様は replay/SPEC.md、経緯は replay/HANDOFF.md）

- 係数・買い目・スコアロジックを1文字でも変更する時は、デプロイ前に必ず実行する:
  ```
  node replay/run.mjs --engine keirin_logic.js
  ```
  過去718レース（2026-05-17〜07-04、実払戻精算）の的中率・回収率が一発で出る。
- 変更前後の両方を計測し、**数字で勝った変更だけ**を採用する。負けたら入れない。
- 検算モード `--verify` で実ログとの買い目一致率も確認できる（基準70%以上）。
- 歴代エンジンとの比較: `--engine replay/engines/<版>.js`。実験結果JSONは `--out` で保存。
- fixtures.json は測定基準ゆえ変更禁止。レースを追加する場合は新ファイルとして拡張する。

背景: 2026年4→6月の成績下落（的中率15.4%→8%台）は、V10.16〜21の買い目改修を
無計測でデプロイしたことが主犯だった（リプレイ台の実測で確定、V10.23で復元済み）。
このルールは同じ悲劇を二度と起こさないための確定フロアである。
